//Programa que pide al usuario el numero de componentes
//y da como resultado el componente del vecotr tres

#include <stdio.h>
#include <stdlib.h>
main()
    {
     system("color 5F");
     int i,n;
     int v1[i];
     printf("Dame el numero de componente: ");
     scanf("%i",&n);
     for(i=0;i<n;i++)
        {printf("Ingresa el valor del vector: ");
        scanf("%i",&v1[i]);}
     for(i=0;i<n;i++)
        v3[i]=v1[i]+v2[i];
        printf("Valor componente %i = %i",i++,v3[i]);
system("pause");
return 0;
}
